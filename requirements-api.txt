# Dependencies for api/ (Partie 1.1 -- multi-tenant auth backend), kept in
# a separate file from requirements.txt (the RAG pipeline's own deps) so
# `pip install -r requirements.txt` still works standalone for anyone only
# touching src/ or dashboard/ -- installing both files together is what a
# full `uvicorn api.main:app` run actually needs.
#
# Exact versions frozen from the environment this was built and tested
# against (Python 3.13, Windows) -- same reasoning as requirements.txt's
# own header: relax the pin and re-run `pytest tests/test_auth_*.py`
# before trusting the result. Passlib is deliberately NOT used for
# password hashing -- see api/security/hashing.py's docstring: it's
# unmaintained and its own backend self-test crashes against current
# bcrypt releases.

# -- Web framework / server -------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
itsdangerous==2.2.0
# CI/CD audit finding: api/services/email.py, api/security/password_strength.py,
# and api/routers/oauth.py all `import httpx` directly, but this file
# previously relied on requirements.txt (the RAG pipeline's own deps) to
# provide it as a side effect of "install both files together" -- true
# for local dev, but requirements-api.txt must be self-sufficient for a
# CI job that only needs api/ and correctly has no reason to install the
# RAG pipeline's dependencies (including one, agentfixture, that isn't
# resolvable from a fresh environment at all -- see its own comment in
# requirements.txt). Same pinned version requirements.txt already uses.
httpx==0.28.1
# httpcore was already present transitively (httpx's own real HTTP/1.1
# implementation) -- pinned directly now that api/services/url_fetching.py
# (Partie 2.1.10) imports its `_backends.anyio.AnyIOBackend` directly,
# the real SSRF-safe-connection extension point httpx itself doesn't
# expose as a public constructor argument -- see that module's own
# docstring for the full real design.
httpcore==1.0.9

# -- Database (PostgreSQL, async) --------------------------------------------
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
psycopg2-binary==2.9.12
alembic==1.19.1

# -- Settings / validation ---------------------------------------------------
pydantic-settings==2.15.0
email-validator==2.3.0
# dnspython was already present transitively (email-validator) -- pinned
# directly now that api/security/custom_domains.py (Partie 1.4.1) imports
# it itself (real, async DNS TXT lookups for domain verification), rather
# than relying on another package's dependency tree to keep providing it.
dnspython==2.8.0

# -- Auth: passwords, JWT, OAuth, 2FA -----------------------------------------
bcrypt==5.0.0
pyjwt[crypto]==2.13.0
authlib==1.8.0
pyotp==2.10.0
qrcode[pil]==8.2
# Pillow was already present transitively (qrcode[pil]) -- pinned directly
# now that api/services/storage.py's branding logo/favicon validation
# (Partie 1.3.10) imports it itself (real pixel-dimension checks), rather
# than relying on another package's dependency tree to keep providing it.
Pillow==12.3.0
# cryptography was already present transitively (pyjwt[crypto]) -- pinned
# directly now that api/security/secret_encryption.py (audit Categorie 4,
# items 27/28) imports it itself, rather than relying on another
# package's dependency tree to keep providing it.
cryptography==50.0.1
# -- WebAuthn / FIDO2 (audit Categorie 4, item 26) ----------------------------
webauthn==3.0.0

# -- ACME / Let's Encrypt (Partie 1.4.3) --------------------------------------
# The same acme/josepy libraries certbot itself uses -- a real ACME v2
# (RFC 8555) client, not a hand-rolled reimplementation of the protocol's
# JWS signing/nonce handling. Pulls in `requests` (sync) -- see
# api/security/ssl_certificates.py's docstring for why its blocking
# calls are wrapped in asyncio.to_thread rather than given an async
# HTTP client of their own.
acme==5.8.0
josepy==2.2.0

# -- Object storage (avatar uploads, 1.1.13) ----------------------------------
boto3==1.43.83

# -- Background jobs (1.1.10's account-purge sweep) --------------------------
celery==5.6.3
redis==8.1.0

# -- Monitoring (audit finding 22) --------------------------------------------
# prometheus-client's multiprocess mode (api/monitoring.py) is the
# actual reason this is a real dependency and not hand-rolled like
# api/security/rate_limit.py or password_similarity.py -- correctly
# aggregating histograms across several worker PROCESSES (mmap'd
# per-worker files, cleaned up as workers come and go) is real,
# easy-to-get-wrong complexity a battle-tested library exists
# specifically to solve, not "a few dozen lines to read in a few
# minutes." gunicorn is Unix-only (uses fcntl -- won't run on Windows,
# though `pip install` itself succeeds there) and only needed to run
# gunicorn.conf.py in actual production; local dev keeps using
# `uvicorn api.main:app --reload` either way.
prometheus-client==0.26.0
gunicorn==23.0.0

# -- RBAC / policy engine (Etape 1.2.7) ---------------------------------------
# PyPI publishes this project under TWO distribution names -- "casbin"
# (legacy, frozen at an old 1.x version line) and "pycasbin" (current,
# what casbin-async-sqlalchemy-adapter itself actually depends on) --
# both install into the same importable `casbin` package, so installing
# both is fragile (whichever installs last silently wins). pycasbin is
# the one actually maintained; api/security/rbac.py's AsyncEnforcer usage
# requires it (pycasbin>=2.x), not the legacy "casbin" line.
pycasbin==2.8.0
# Async, not the sync casbin-sqlalchemy-adapter the original ask named --
# this codebase's entire DB layer (api/database.py) is async SQLAlchemy;
# the sync adapter would need its own separate sync engine talking to the
# same Postgres instance outside SQLAlchemy's async session management,
# the same category of footgun documented elsewhere in this codebase
# (MissingGreenlet, sync lazy-loads under an async session).
casbin-async-sqlalchemy-adapter==1.17.0

# -- Documents / PDF import (Partie 2.1.1) ------------------------------------
# The exact library the master cahier des charges names for this item
# ("2.1.1 | PDF | pymupdf (fitz)") -- verified for real (against a real
# generated PDF, not assumed from docs) to cover text, real table
# detection (page.find_tables()), metadata, AND embedded images all on
# its own, so no separate table-extraction library (pdfplumber, camelot)
# is needed. Imported as `pymupdf`, not the classic `fitz` alias --
# this version deprecates it.
pymupdf==1.28.2
# extract_pdf_tables returns one DataFrame per table (this step's own
# literal return type).
pandas==3.0.5
# -- Embeddings for document chunks (also Partie 2.1.1's own
# process_document spec, "chunking + embedding") -- the SAME model
# already configured as organization_settings.embedding_model's default
# (Partie 1.3.9) and already used by src/indexing.py for the RAG demo
# pipeline, now actually READ and used for the first time by api/ (see
# api/security/documents.py's own module docstring on the honest scope
# of this relative to Partie 4's still-⬜ multi-provider abstraction).
# Same CPU-only wheel trick as requirements.txt's own torch pin -- an
# unpinned transitive dep here would resolve pip's default CUDA-enabled
# build, pulling several GB of unused nvidia-*-cu12 packages this
# CPU-only API service never needs.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
sentence-transformers==5.7.0

# -- Documents / DOCX import (Partie 2.1.2) -----------------------------------
# The exact library the master cahier des charges names for this item
# ("2.1.2 | DOCX | python-docx"). Pulls in lxml (already needed for
# real XML parsing of the OOXML package's internal parts) as its own
# transitive dependency.
python-docx==1.2.0

# -- Documents / TXT import (Partie 2.1.3) ------------------------------------
# charset-normalizer was already present transitively (acme -> requests)
# -- pinned directly now that api/services/txt_extraction.py imports it
# itself for real automatic encoding detection, same "pin what you
# actually import" convention as dnspython/Pillow/cryptography above.
charset-normalizer==3.5.1

# -- Documents / Markdown import (Partie 2.1.4) -------------------------------
# markdown-it-py and PyYAML were already present transitively (rich ->
# sentence-transformers's own dependency chain; huggingface_hub/
# transformers) -- both pinned directly now that
# api/services/markdown_extraction.py imports them itself, same "pin
# what you actually import" convention as every other entry in this
# section. mdit-py-plugins (the official markdown-it-py extension
# package, same maintainers) is a genuinely new dependency -- CommonMark
# alone does not parse GFM tables or YAML frontmatter at all, confirmed
# for real before relying on either.
markdown-it-py==4.2.0
mdit-py-plugins==0.6.1
pyyaml==6.0.3

# -- Markdown rendering / HTML sanitization (Partie 8.1.2) --------------------
# A real, genuinely new dependency -- markdown-it-py above is already
# real (used for real Markdown INGESTION, 2.1.4), reused here directly
# for real RENDERING too, but this codebase has no real HTML
# sanitizer anywhere yet. bleach is the real, standard, well-known
# choice (an allowlist-based sanitizer, not a denylist one) for this
# exact real XSS-prevention need.
bleach==6.2.0

# -- Conversation export / PDF (Partie 8.1.14) --------------------------------
# python-docx (DOCX export) and markdown-it-py+bleach (Markdown export)
# above are already real dependencies, reused directly -- weasyprint is
# the one genuinely new one, for real PDF export. Real, DOCUMENTED
# system dependency (same category as pytesseract/pdf2image, Partie
# 3.1.6): weasyprint needs real native Pango/cairo/GObject libraries a
# plain `pip install` does not provide -- confirmed directly (`import
# weasyprint` itself raises a real OSError without them). CI installs
# them via apt-get (see .github/workflows/regression.yml); a real
# local dev machine missing them still imports this whole app fine
# (api/services/conversation_export.py imports weasyprint LAZILY,
# inside export_to_pdf only) -- only an actual PDF export attempt
# fails, with a real, honest ExportError instead of a crash.
weasyprint==63.1

# -- Documents / HTML import (Partie 2.1.5) -----------------------------------
# The exact libraries the master cahier des charges names for this item
# ("2.1.5 | HTML | BeautifulSoup4 + readability-lxml"). beautifulsoup4
# and lxml were already present transitively (the `unstructured` /
# `pikepdf` dependency chains) -- pinned directly now that
# api/services/html_extraction.py imports/relies on them itself, same
# "pin what you actually import" convention as every other entry in
# this section. readability-lxml (the real arc90-readability-algorithm
# port, verified for real to correctly separate article body from
# nav/header/footer boilerplate before relying on it) is a genuinely
# new dependency; it in turn needs lxml[html-clean]'s lxml_html_clean
# (lxml 5+ split its old, deprecated html.clean module out into this
# separate package), pulled in automatically.
beautifulsoup4==4.15.0
lxml==6.1.3
readability-lxml==0.9

# -- Documents / CSV import (Partie 2.1.6) ------------------------------------
# The exact library the master cahier des charges names for this item
# ("2.1.6 | CSV | pandas") is ALREADY pinned above (Partie 2.1.1's own
# `pandas==3.0.5`, for PDF table extraction) -- no new pin needed, and
# deliberately not re-listed here to avoid a duplicate requirement line.
# Delimiter detection (api/services/csv_extraction.py's
# detect_csv_delimiter) uses Python's own stdlib `csv.Sniffer` -- no
# dependency at all, real and battle-tested for exactly this job.

# -- Documents / JSON import (Partie 2.1.7) -----------------------------------
# api/services/json_extraction.py uses ONLY Python's own stdlib `json`
# module (C-accelerated) -- no new dependency. A full JSONPath query
# engine (e.g. jsonpath-ng), which the master cahier des charges'
# one-line description for this item ("Parsing récursif configurable
# (JSONPath)") could be read as implying, is deliberately NOT added:
# none of this step's own literal action items ask for path-based
# querying -- see that module's own docstring for the full reasoning.

# -- Documents / XML import (Partie 2.1.8) ------------------------------------
# The exact library the master cahier des charges names for this item
# ("2.1.8 | XML | lxml") is ALREADY pinned above (Partie 2.1.5's own
# `lxml==6.1.3`, a transitive need of readability-lxml) -- no new pin
# needed. `xmltodict` (vision critique's own named alternative) was
# installed temporarily to compare its real output format against
# this module's own hand-written dict conversion, then removed --
# never a real dependency, see api/services/xml_extraction.py's own
# module docstring for the full reasoning, including the real
# entity-expansion DoS finding that shaped this module's parser
# configuration.

# -- Documents / EPUB import (Partie 2.1.9) -----------------------------------
# The exact library the master cahier des charges names for this item
# ("2.1.9 | EPUB | ebooklib") and this step's own vision critique
# question 5 -- confirmed for real to raise a genuinely unpredictable
# mix of exception types for corrupt input (its own EpubException, a
# bare KeyError, or a bare AttributeError depending on exactly what's
# wrong), the same real shape of finding Partie 2.1.2's own DOCX
# extraction hit with python-docx -- see api/services/epub_extraction.py's
# own module docstring. Pulls in `six` as its own transitive dependency
# (not pinned separately -- nothing here imports it directly). Chapter
# HTML-to-text reuses `beautifulsoup4` + `lxml`, already real
# dependencies since Partie 2.1.5.
ebooklib==0.20

# -- Documents / OCR (Partie 3.1.6) --------------------------------------
# Both are thin Python wrappers around a REAL, SEPARATE system binary
# neither package installs itself: pytesseract shells out to the real
# `tesseract` CLI (Tesseract OCR engine), pdf2image to `pdftoppm`/
# `pdftocairo` (poppler) to rasterize a PDF page before it can be OCR'd
# at all. Neither binary is a Python package -- see .github/workflows/regression.yml's
# own api-tests job for the real `apt-get install tesseract-ocr poppler-utils`
# CI step, and a real production deployment needs the equivalent for its
# own OS. Pillow (already pinned above, Partie 1.3.10) is what pytesseract
# itself uses to load an image before handing it to the real binary.
pytesseract==0.3.13
pdf2image==1.17.0

# -- Documents / language detection (Partie 3.1.7) -----------------------------
# A pure-Python port of Google's own language-detection library -- no
# system binary, no model file to download (unlike fasttext's own
# ~130MB compressed model, the literal spec's alternative choice) --
# a real, deliberate, lower-risk pick for a metadata-enrichment
# feature this codebase's own CI/dev environment shouldn't need extra
# infrastructure for. Non-deterministic between runs unless seeded
# (a real, documented quirk of its own underlying Naive Bayes
# algorithm) -- api/services/language_detection.py's own module seeds
# it once at import time.
langdetect==1.0.9

# -- Query rewriting (Partie 3.4.2) --------------------------------------------
# A real, new, genuinely justified dependency -- unlike most other
# Partie 3.4 modules (which all found something already reusable in
# this codebase), no real spell-checking capability existed anywhere
# yet. Real, pure-Python, bundled real word-frequency dictionaries (EN
# + FR, no separate download at runtime, no system binary) -- the
# exact "symspell ou pyspellchecker" choice this étape's own literal
# text offers, picked for its simpler real API.
pyspellchecker==0.9.0

# -- Retrieval pipeline (Partie 3.3.4-3.3.6) -----------------------------------
# rank-bm25 was already a real dependency of requirements.txt (the RAG
# pipeline's own src/retrieval.py), but NOT of this file -- the exact
# same real packaging gap this file already documents for pygments
# below. Pinned directly now that api/services/retrieval_pipeline.py
# imports it itself, for a genuine api-only production install.
rank-bm25==0.2.2

# -- Chunking / code-aware tokenization (Partie 3.2.5) -------------------------
# A real, latent packaging bug found while building this étape: Pygments
# was already IMPORTABLE in this dev environment, but only transitively
# -- via `pytest` (a test-only dependency) and `rich` (itself only
# transitive, via `typer`, never declared here at all) -- so a genuine
# production `pip install -r requirements-api.txt` (no dev/test extras)
# would NOT reliably have it, silently breaking
# api/services/code_chunking.py's own real per-language tokenizer/
# language-detection fallback the first time it actually ran. Pinned
# directly now that that module imports it itself -- the same real
# pattern this file already applies to dnspython/httpcore/Pillow above.
pygments==2.21.0

# -- Documents / URL import (Partie 2.1.10) -----------------------------------
# No genuinely new top-level package -- httpx (real fetch), lxml +
# beautifulsoup4 + readability-lxml (real content/metadata extraction,
# reusing Partie 2.1.5's own HTML extraction cores) are all already
# real dependencies above. httpcore is now pinned directly (see its
# own entry above) for the real SSRF-safe transport this step needed.

# -- Documents / Sitemap import (Partie 2.1.11) -------------------------------
# No new package at all -- real sitemap XML parsing reuses Partie 2.1.8's
# own lxml (SAFE_XML_PARSER, api/services/xml_extraction.py) and Partie
# 2.1.10's own SSRF-safe fetch_url_content; gzip decompression and glob
# filtering use Python's own stdlib (gzip, fnmatch), nothing to pin.

# -- Documents / GitHub repo import (Partie 2.1.12) ---------------------------
# No new package at all -- a plain httpx.AsyncClient (already a real
# dependency since Partie 2.1.10) against GitHub's own REST API, no SSRF-
# safe transport needed (see api/services/github_extraction.py's own
# module docstring for why: the real target host is always the fixed,
# admin-configured GITHUB_API_BASE_URL, never caller-controlled). Base64
# decoding uses Python's own stdlib.

# -- Documents / GitHub issues import (Partie 2.1.13) -------------------------
# No new package at all -- extends Partie 2.1.12's own github_extraction.py
# with more calls against the same real GitHub REST API, same httpx
# client, same reasoning for no SSRF-safe transport.

# -- Documents / Google Drive import (Partie 2.1.14) ---------------------------
# No new package at all -- a plain httpx.AsyncClient (already a real
# dependency) against Google's own fixed OAuth token endpoint and Drive
# API v3 host, no SSRF-safe transport needed (same reasoning as GitHub's
# own import above: the real target host is always fixed, never
# caller-controlled). Deliberately NOT google-api-python-client/
# google-auth -- Drive's own real REST surface this step needs (OAuth
# refresh-token exchange, files.list/get, alt=media download) is a
# handful of plain HTTP calls, the same "no heavy SDK without a real,
# expressed need" restraint as every prior import step's own library
# choice (no Playwright, no git clone, no Search API).

# -- Multi-provider LLM abstraction (Partie 4.1.1-4.1.7) -----------------------
# A single real library covering all 6 real providers this batch of
# étapes asks for (Anthropic, OpenAI, Gemini, Mistral, Ollama, any
# OpenAI-compatible endpoint) -- litellm makes its own real HTTP calls
# per provider internally, so it deliberately does NOT need
# google-generativeai/mistralai/openai as SEPARATE SDK dependencies
# each individual étape's own literal text names; the SAME "no heavy
# SDK without a real, expressed need" restraint every other import step
# in this file already applies (see e.g. the Google Drive section
# above). `anthropic` (already a real dependency of requirements.txt,
# the RAG pipeline's own src/generation.py) is likewise not needed here
# for the same real reason.
litellm==1.99.0

# -- Tests (tests/test_auth_*.py) ---------------------------------------------
aiosqlite==0.22.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
