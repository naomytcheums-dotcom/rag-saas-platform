.env
__pycache__/
*.pyc
.venv/
venv/
data/chroma/
data/raw/
data/telemetry.jsonl
.streamlit/secrets.toml
.claude/
dump.rdb
.coverage
htmlcov/

# Local observability stack (docker-compose.observability.yml) -- bind-mounted
# data, if ever added instead of the named Docker volumes this file uses today.
observability/data/
observability/*/data/

# Local-only CLI tools downloaded during setup (e.g. abctl for Airbyte) --
# real binaries, never source, never committed.
.tools/
snyk.exe

# Real, downloaded ML model weights (Partie 22 finalization -- YOLOv8n
# via ultralytics, api/services/object_detection.py) -- fetched at
# runtime from ultralytics' own GitHub releases, never committed, same
# reasoning as .tools/ above.
storage/ml_models/
