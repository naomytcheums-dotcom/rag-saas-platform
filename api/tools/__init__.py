"""Partie 5.2 -- real, integrated agent tools, built on top of the
minimal `ToolSpec` abstraction from api/services/tools.py (Partie
5.1.2). Distinct from that module's own two demo tools (`calculator`/
`word_count`): everything under `api/tools/` wraps this codebase's own
real, already-live infrastructure (retrieval, GitHub, calendars, ...)."""
